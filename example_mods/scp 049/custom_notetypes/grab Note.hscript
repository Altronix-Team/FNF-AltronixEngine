function onCreate()
{
    setNoteTypeTexture('grab Note', '049NOTE');
    setNoteTypeIgnore('grab Note', true);
}

function goodNoteHit(a, b, c, d)
{
    if (c == 'grab Note')
    {
        setProperty(null, 'health', 0);
    }
}